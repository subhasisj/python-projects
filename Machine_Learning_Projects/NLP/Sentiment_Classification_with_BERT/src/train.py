class training:

    def __init__(self,data_loader,model,optimizer,device):
        
        self.data_loader = data_loader
        self.model = model
        self.optimizer = optimizer
        self.device = device
    

    def train():

        model.train()

        for batch, data in 